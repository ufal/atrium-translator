# requirements-eval.txt — optional dependency group for eval/bakeoff.py (issue #4).
#
# The harness runs without these (it falls back to reference-less heuristic QE and
# terminology hit-rate), but installs them to enable reference-based scoring:
#   - sacrebleu : chrF / BLEU when --refs is supplied
#   - unbabel-comet : COMET / COMET-QE (heavy; pulls torch) when available
#
#     pip install -r eval/requirements-eval.txt
sacrebleu>=2.6.0
# unbabel-comet>=2.2.0   # uncomment for COMET / COMET-QE (large download, needs torch)
