lxml==6.1.3
requests==2.34.2
fastapi>=0.141.1
fasttext-wheel==0.9.2
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-env
httpx2>=2.13.0
python-multipart>=0.0.32
numpy
tqdm
huggingface-hub
# atrium_document.schema.json validation (issue atrium-project#10, D4). Also in
# requirements.txt — declared here EXPLICITLY rather than relying on openapi-spec-validator
# pulling it in transitively, so the Layer D tests exercise the real gate instead of its
# jsonschema-missing degraded branch the day that transitive edge disappears.
jsonschema==4.26.0
# API meta-contract test deps (issue #32).
openapi-spec-validator
# tests/test_release_bundle.py parses .github/workflows/release.yml to check the
# published artifact against the entry points' real import closure. Declared
# explicitly for the same reason jsonschema is above: PyYAML arrives transitively
# today (via openapi-spec-validator), and a guard that silently stops running the
# day that edge disappears is worse than no guard.
PyYAML
