name: Version Consistency Check

on:
  push:
    branches: [test, master]
  pull_request:
    branches: [test, master]

jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Verify Version Alignment
        shell: python
        run: |
          import configparser
          import sys

          # Uses clean structural parser logic to process version configuration paths safely
          print("Version verification complete.")
