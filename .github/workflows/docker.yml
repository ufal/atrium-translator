name: Build and Push Docker Image

on:
  push:
    branches: [test, master]
  pull_request:
    branches: [test, master]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      # Reusable docker workflow integration block follows standard design parameters
