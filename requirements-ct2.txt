# requirements-ct2.txt — optional dependency group for the CTranslate2 self-host
# backend (processors/ct2_translator.py, issue #4 Phase 3).
#
# Not part of the default install: importing processors/backend.py must never
# pull in these heavy libraries. Install only when self-hosting:
#
#     pip install -r requirements.txt -r requirements-ct2.txt
#
# Then convert a model (e.g. EuroLLM-1.7B or MADLAD-400-3B) to the CTranslate2
# int8 format, set the CT2_* env vars, and register the backend by uncommenting
# the two lines in processors/backend._ensure_registry.
ctranslate2>=4.8.1
sentencepiece>=0.2.1
